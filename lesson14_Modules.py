import math
import sys
import random as rdm # rdm is alias
from enum import Enum

print(math.pi)
rdm.choice("123")


print(dir(rdm)) # dir shows what is inside module

for item in dir(rdm):
    print(item)

